package p21

use p21@npinto21/p21-sqlite@v1 as db

conn := db.Open({
    path: "./.recovery/modules/npinto21/sqlite/tests/db/alias_interpreter.db"
})
state := db.State(conn)

@"driver=%state.driver, open=%state.open"\

db.Close(conn)
